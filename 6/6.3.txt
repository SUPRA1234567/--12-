#включати <stdio.h>
//#include <conio.h> // Видалив, після conio.h не є стандартною бібліотекою в багатьох компіляторах
#визначити СТІЙ'*'

внутр основний() {
    char гл;
    гл = getchar();
    
    мл:якщо(гл != СТІЙ) {
        putchar(гл);
        гл = getchar();
        goto ml;}

    повернення 0;
}